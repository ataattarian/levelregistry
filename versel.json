{
    "builds": [
        {
            "src": "levelCampBackend/wsgi.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "levelCampBackend/wsgi.py"
        }
    ]
}